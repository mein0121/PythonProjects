import testing


# canvas = "T"
# draw_arr = "m"
# label = "svm"
# testing.save_file(canvas,draw_arr)

# testing.save_file(canvas, draw_arr, label)

# testing.save_file(canvas,draw_arr)

# testing.save_file(canvas, draw_arr, label)

con = [['t','f'],'a','b','vc']

print(con[1], con[0])